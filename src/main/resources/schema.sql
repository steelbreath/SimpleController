create table Users(
                      id LONG NOT NULL AUTO_INCREMENT,
                      name VARCHAR(100) NOT NULL,
                      ipn LONG NOT NULL,
                      PRIMARY KEY ( id )
);